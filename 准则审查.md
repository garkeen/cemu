# cemu 准则审查（阶段 1 代码）

逐文件审查阶段 1 全部源码的结果。每条含：位置、违反类型、说明、参考出处、处置与结果。
处置分三类：修（代码改动）、登记（属于简化，进简化登记表）、保留（规格允许的设计决定）。
**状态（2026-08-30）：A/B/C 各条已全部处置完毕，127 项套件回归全绿，构建零告警。**

## A. 死代码 —— 已全部修复

**A1. execute.c / fp.c 两份重复的 fp 操作枚举（含 kFpSgnj/kFpSgnjn/kFpSgnjx 无引用值）**
处置：修。枚举上移到 riscv.h 单一来源（kFpAdd..kFpDiv、kFpEq/kFpLt/kFpLe），删除两份本地副本与三个无引用值。✅

**A2. fp.c `Sgnj32()`**
处置：修。删除（Sgnj64 仍被 RiscvFpSgnjD 使用，保留）。✅

**A3. elf.c `kShtStrtab` 常量、`FindHtifSymbols()` 的 `Bus *bus` 形参、两处 `UNUSED`、死局部变量 `link`**
处置：修。形参、常量、UNUSED、死局部全部删除，调用点同步。✅

**A4. execute.c Exec32 case 0x53 的 `is_double` 局部变量与 `UNUSED(is_double)`**
处置：修。删除。✅

**A5. device/sifive_test.c 已编译但零接线（SifiveTestRegister 无调用者）**
处置：修。Makefile `filter-out` 排除出构建，machine.h 去掉 include 与 `SifiveTestDevice test` 字段；文件保留在树中（无版本控制），machine/virt.c 落地时恢复。✅

**A6. fp.c RoundSAdjust 内 `getenv("CEMU_FPDBG")` 调试残留**
处置：修。审查登记时确认该块已在此前清理中删除，当前源码无残留。✅

**A7. host/time_win.c `HostTimerNow()` 零调用者**
处置：修。host.h 去掉声明，Makefile 排除 time_win.c；阶段 2 CLINT mtime 需要宿主时钟时恢复接线。✅

**A8. type.h `UNUSED` 宏失去全部使用方**
处置：修。删除宏；sifive_test.c（树中保留、构建排除）改用 `(void)` 投影。✅

**A9. RiscvState.machine 死字段 + MachineGetBus() 纯转发访问器**
machine 字段只在 Init 赋值、从不被读；访问器是纯转发，而 riscv.h 本就 include core/machine.h，封装是假的（准则四 + 准则一违反）。
处置：修。CpuState 直接持 `Bus *bus`（只读总线一件事），RiscvState 去掉 machine 字段，删除 MachineGetBus；ISA 对 core 的依赖面从 core/machine.h 收窄到 core/cpu.h + core/bus.h。✅

## B. 硬编码 —— 已全部修复

**B1. csr.c `ReadCounter()`：time CSR(0xC01) 别名为 mcycle**
简化实现。time 应来自真实计时源。
参考：xiangshanNEMU system/timer.c 通过 init_mtimer_regs 注入 CLINT mtime 指针；dearchap-tinyemu riscv_machine.c clint_read() 在 0x02000000 提供 mtime（mtime 在 CLINT 偏移 0xbff8）。
处置：登记（D4）。阶段 2 CLINT 落地时注入。

**B2. csr.c `tcontrol` 写掩码魔数 `& 0xc0`**
处置：修。riscv.h 定义 `kTcontrolMte`(1<<7)/`kTcontrolMpte`(1<<6)（RISC-V 调试规范 tcontrol 位段；xiangshanNEMU encoding.h 只声明了 CSR 号未实现位段）。✅

**B3. execute.c 计数器门控 `mcountinhibit >> 2` / `>> 0` 内联**
处置：修。riscv.h 定义 `kMcountinhibitCy`(1<<0)/`kMcountinhibitIr`(1<<2)（特权规范 mcountinhibit 位段）。✅

**B4. mstatus 位操作散落 + WARL 逻辑无名**
原：MIE/MPIE/MPP/FS/UXL 全部内联魔数。
参考：xiangshanNEMU priv.c 写路径统一 `mask_bitset(old, WMASK, src)`，MPP 保留值恢复 prev_mpp（priv.c:2554-2565）。
处置：修。riscv.h 集中定义 kMstatusMie/Mpie/MppMask/Fs/UxlMask/UxlValue；RiscvTrap/RiscvMret/CsrRead(0x100)/CsrWrite(0x100,0x300)/FpEnabled/MarkFpDirty 全部改用命名常量。MPP 的拒绝分支保留（规格 3.1.6：xPP 只能持有 M 与已实现的更低模式，S/H 未实现；xiangshan 同形）。
附带修复：mepc[1] 的 WIRI 语义。原实现只在 mret 取目标时掩，读回 mepc 不掩（gem5 isa.cc:557-569 在读路径掩 C 关闭时 `mbits(val,63,2)`）。登记为 E3，随阶段 2 CSR 表驱动化统一处理。✅（位段命名部分）

**B5. `MisaSupported()` 与 `RiscvInit` 两处重复定义 misa 字母集**
参考：gem5 isa.cc:368-425 初始化处单一派生。
处置：修。riscv.h 定义 `kMisaSupported`（kMisaMxl64/A/C/D/F/I/M 组合），两处共用；csr.c 0x301 写路径的 C 位判断同步用 kMisaC。✅

**B6. loader.c `out->fromhost = bin_tohost + 0x40` 魔数**
处置：修。loader.c 定义 `kBinFromhostOffset = 0x40`，注明出处 riscv-test-env p/riscv_test.h 的 `.align 6` tohost/fromhost 段布局（probe 亦验证）。✅

**B7. main.c 默认值（256MB、0x80000000、0x80001000）内联在参数解析**
参考：dearchap-tinyemu riscv_machine.c:66 布局常量在机器文件。
处置：修。machine.h 定义 kSpikeDramBase/kSpikeDramSizeDefault/kRawBinTohostOffset，main.c 只引用；Usage 同步。✅

**B9. csr.c getenv 隐式声明（缺 stdlib.h）+ RiscvMret/ExecFma 未用形参**
处置：修。补 include；RiscvMret 去掉 cpu 形参（从未使用），ExecFma 同。✅

## C. 特判 —— 已全部修复

**C1. htif.c tohost 写路径三条分支 + 低字立即退出分支**
参考：riscv-test-env p/riscv_test.h:215-218 确认退出序列为 `sw TESTNUM, tohost; sw zero, tohost+4` 自旋；fesvr 的本质契约是宿主轮询 tohost 非零即处理；dearchap riscv_machine.c htif_write 单通道（低字组装、高字触发统一 handle_cmd）。
处置：修。重写为单路径："组装写出的字节到影子寄存器，非零即处理并清零"，等价 fesvr 轮询语义，同时覆盖 64 位 sd 设备命令与 32 位 sw 退出对，零分支特判。fromhost 应答从恒 1 改为 fesvr/dearchap 语义 `(dev<<56)|(cmd<<48)`（见 D10）。✅

**C2. core/bus.c `BusRamRange()` 用 `strcmp(ops->name, "ram")` 识别 RAM**
参考：dearchap-tinyemu iomem.h:51 `BOOL is_ram` 字段 + iomem.c:89/200 注册路径置位 + :245 取指针对前置检查（PhysMemoryRange 自带 phys_mem 直指）。
处置：修。BusRegion 增 `host` 直指字段（NULL 即 MMIO），新增 BusAddRamRegion 注册 RAM（machine.c 传入 ram->mem），BusAddRegion 专管 MMIO；BusRamRange 判字段取指针，bus.c 不再 include 任何设备头。✅

**C3. riscv.h `pending_next_pc` 字段**
检查本身是规格要求（misa WARL：清 C 不得使下一次取指非对齐；gem5 isa.cc:887-905 同款检查，取 npc 判断）。
处置：修。RiscvCsrWrite 增 `next_pc` 形参（调用点 ExecSystem 本来就有），删除字段与为其赋值的语句。✅

**C4. riscv.h `suppress_inc` 字段 —— 审查时判的"两个真 bug"经探针重新裁决**
原审查判定：a) csrwi mcycle,0 后读到 1 违反规格；b) csrwi mcycle 步 minstret 少计 1。
**裁决过程**（test/riscv64/probe/probe_counters.S，留作证据）：
- QEMU `-M spike -icount shift=0` 打印 `110`：mcycle 写入不自抑制（下一条读到 1）、minstret 写入自抑制（读到 0，与官方测试一致）、minstret 被mcycle 写连带冻结。
- QEMU 无 icount 时 mcycle 读到 9 —— TCG 计数器按 TB 记账，非逐条精确，逐位对齐无意义，逐位模仿不可取。
- 官方测试只锚定 minstret 写入自抑制（rv64mi-p-instret_overflow.S:25-27 明文"写入的值即下一条读到的值——本步自增被抑制"）；同文件 instreth 注释表明抑制规则是"写哪个计数器抑制哪个"的一般规则；对 mcycle 无测试。
**最终语义**（按规格对称，手册裁决）：写哪个计数器，就抑制哪个自身的本步自增；无跨计数器连累。
- csrwi mcycle,0 → 下一条读 0（恢复原审查判定 a，与规格对称一致；QEMU 在此与规格分歧，QEMU 只实现了有测试覆盖的 minstret 一侧）。
- csrwi mcycle 不再冻结 minstret（原判定 b 的"少计"消除；QEMU 的冻结行为是 icount 实现副产物，无规格/测试依据，不模仿）。
处置：修。`suppress_inc` 改为 `counter_written`（kCounterNone/kCounterMcycle/kCounterMinstret），CsrWrite 记录写了哪个计数器，Step 按记录精确抑制对应计数器的自增。127 项全绿（instret_overflow 锚定情形不变）。✅

**C5. loader.c `PickIsa()`：--isa 与 ELF e_machine 冲突时静默覆盖**
处置：修。新建 isa/registry.c `kIsaTable`（NULL 结尾注册表，未来 ISA 在此追加），FindIsaByMachine 用 `ops->elf_machine` 匹配（顺带消灭 elf.h `kEmRiscv` 与 isa_riscv64.c `243` 的双源）。ELF 恒自识别；--isa 仅对 raw bin 生效（默认 riscv64，loader.c `kDefaultIsaName`）；ELF + 显式 --isa 不一致报错退出。main.c 的 --isa 默认值改为 NULL 以区分"显式给出"与"未给出"。✅

## D. 简化登记（规格行为缺失，按阶段恢复）

| 编号 | 位置 | 缺失 | 参考出处 | 恢复阶段 | 状态 |
|---|---|---|---|---|---|
| D1 | csr.c satp/stvec/sscratch/sepc/scause/stval/medeleg/mideleg/mcounteren 仅存储 | S 态机制不生效 | xiangshanNEMU priv.c/mmu.c 全实现 | 阶段 2（OpenSBI 硬需求） | 登记中 |
| D2 | execute.c `sfence.vma`/`sret` 判非法 | 同上 | 同上 | 阶段 2 | 登记中 |
| D3 | execute.c `wfi` 视作 nop | 无等待中断语义 | xiangshanNEMU priv.c:3759 `riscv64_priv_wfi` 真实现 | 阶段 2 接 CLINT 后 | 登记中 |
| D4 | csr.c time 别名 mcycle | 无真实计时源 | xiangshanNEMU timer.c mtime 注入；dearchap clint_read（mtime 在偏移 0xbff8） | 阶段 2 | 登记中 |
| D5 | csr.c pmpcfg/pmpaddr 仅存储，无访存执行 | PMP 完全不生效 | xiangshanNEMU reg.c/difftest 含 PMP | 阶段 2（OpenSBI 硬需求） | 登记中 |
| D6 | csr.c tdata1/2/3 WARL 0 | 无 debug trigger（gem5 tselect 写 val+1 报告存在 trigger，cemu 存储） | xiangshanNEMU trigger.c 完整实现 | 阶段 3+（gdb stub 时） | 登记中 |
| D7 | execute.c LR/SC 单核预留集 | 无多核冲突语义 | spike 单核同款；规格允许 SC 假失败 | 多核引入时 | 登记中 |
| D8 | execute.c 中断投递按 bit15..0 顺序 | 未按规格优先级表 | 特权手册中断优先级章节 | 阶段 2 PLIC/CLINT | 登记中 |
| D9 | elf.c 仅 ELF64 | x86 阶段需 ELF32 | gem5/qemu 加载器 | 阶段 4 | 登记中 |
| D10 | htif.c fromhost 应答 | ~~恒置 1~~ **已修**：现按 fesvr/dearchap 语义置 `(dev<<56)|(cmd<<48)`，仅未停机时 | dearchap riscv_machine.c:144 | — | ✅ 随 C1 修复 |
| D11 | htif.c dev0/cmd0/payload&1==0（HTIF syscall）报错退出 | 无 fesvr syscall 设备（sys_write/exit 经内存块） | fesvr htif_t::handle_syscall | 阶段 3 cesdk | 登记中 |
| D12 | x86 execute.c `hlt` 停机无唤醒源 | 无 8253 PIT/8259 PIC 周期中断；QEMU 默认 IRQ0 唤醒 hlt 跑完 realmode 后 41 项 | qemu/intel 8253+8259；dearchap 设备模型 | 阶段 2 PC 平台 | ✅ 阶段2销账：device/i8259.c（QEMU i8259 语义，主从级联 0x20/0xA0，ICW/OCW/优先级轮转/auto-EOI）+ device/i8254.c（QEMU i8254 语义，0x40-0x43，ch0→IRQ0，宿主时钟懒锚定——计数只随 CPU 运行推进）+ IsaOps.RaiseIrq 接口 + CpuState.int_ack（INTA 取向量）+ execute.c INTR 采样/SDM inhibit 窗口/hlt→wait。realmode 全套件 122 PASS/0 FAIL（此前可达 69） |
| D13 | x86 execute.c LMSW/INVLPG/RDTSC/CMPXCHG/CMPXCHG8B/x87FPU 判非法(#UD) | 386 子集外指令与 FPU 未实现 | intel SDM vol.2；QEMU translate.c | 阶段 4 全 x86 | 登记中。现实表现：realmode 套件尾 test_fninit 的 fninit→#UD→套件垃圾 IVT[6]→死循环（run.sh 以 timeout+输出判据容纳；QEMU 参考自身也在更早处因同族原因停滞于 110 行） |
| D14 | x86 execute.c iret/popf 载入屏蔽 RF(bit16)/VM(bit17) | RF 按瞬态建模（真机经 pushfl 不可观测为 1，等价）；VM86 模式不进入（实模式专用模型） | intel SDM EFLAGS；realmode rflags.rf 用例即此 | 阶段 4（VM86 时） | 登记中 |

## E. 自创方案与结构对照

**E1. fp.c 用宿主 float/double/long double 模拟 IEEE 语义 —— 唯一的大块自创实现**
F/D 七套件通过是行为证据，但结构上偏离参考：QEMU 与 spike 都用 Berkeley softfloat，逐运算显式管理舍入与标志。宿主 long double 方案存在 x87→float 双舍入的长尾风险。
处置：登记。两个选项：a) 保留，加 softfloat 测试向量做回归对照；b) 移植 QEMU fpu/softfloat.c（BSD）。倾向先 a，Linux 阶段出现浮点偏差再 b。

**E2. execute.c 单文件混装 I/M/A/C/F/D + csr.c 大 switch**
参考：xiangshanNEMU 按扩展拆 instr/rvi、rvm、rva、rvf、rvd、rvc；CSR 用 per-CSR 处理器表组织（priv.c，写路径统一 mask_bitset + WMASK）。
处置：登记为重构项，与 B4 余项（mepc[1] WIRI）、阶段 2 S 态 CSR 一起表驱动化，避免二次返工。

**E3. mepc[1] WIRI（IALIGN=32 时读回应为 0）只在 mret 目标处掩，读路径未掩**
参考：gem5 isa.cc:557-569 读路径掩；QEMU 写路径掩。无测试覆盖。
处置：登记，随 E2 表驱动化统一（倾向写路径掩：写入时 C 关闭即清 bit1）。

## 处置顺序（已执行完毕）

1. ✅ A1–A8 死代码（纯删除 + Makefile 排除）。
2. ✅ C1/C2/C4（htif 单通道、is_ram→host 直指、计数器抑制精确化——含探针裁决）。
3. ✅ C3/C5/B2–B7/B9（通道正规化、ISA 注册表、命名常量、机器布局常量）。
4. D 表随阶段 2 各项落地逐条销账（D10 已随 C1 完成）。
5. E1/E2/E3 在阶段 2 间歇期做。

## 回归证据

- `test/run.sh`：127 项 riscv-tests（ui 54/um 13/ua 19/uc 1/uf 11/ud 12/mi 17）全部通过。
- 构建：MinGW gcc 8.1，`-Wall -Wextra` 零告警。
- 探针（test/riscv64/probe/）：probe_counters.elf cemu `000` / QEMU icount `110`（分歧已注明）；probe_bin.bin 冒烟 .bin 加载路径（默认 --htif 0x80001000、fromhost +0x40）通过。
