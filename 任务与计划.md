# cemu 任务与计划

## 一、目标

1. **cemu**：纯 C、纯 Win32 API 的解释型全系统模拟器。多 ISA：riscv64 先行，之后 x86 早期、arm 早期、mips32。加载 .bin/.elf/.img，模拟真实机器契约，运行真实软件。
2. **cesdk**：AM 式构建系统，C 源码 → 自含 .bin/.elf/.img，产物在真 QEMU 上可运行。
3. 顺序：cemu 先行，cesdk 第三阶段。理由：cemu 的测试阶梯不依赖 cesdk；cesdk 只能靠在 cemu 上跑输出来测；cesdk 的 API 要等真实软件跑过才有设计依据。

## 二、铁律

以 开发准则.md 为准（禁止硬编码/简化实现/特判/死代码，参考优先，反案例驱动）。
违规项的登记与销账在 准则审查.md 跟踪。

- 反案例导向：验收件只能是官方测试套件或真实开源软件，禁止为 cemu 定制的程序充当验收件。
- 通用性靠结构：core 不 include 任何 isa 的头文件；windows.h 只允许出现在 host/；设备不感知 CPU 型号；机器定义与 ISA 正交。
- 不造协议：机器按 spike 与 QEMU virt 的真实契约；退出走 HTIF 或 sifive_test。
- 命名：Google C 风格。文件与变量 snake_case，类型与函数 PascalCase，常量与枚举值 k 前缀，宏全大写。

## 三、目录结构

```
cemu/src/
  main/          main.c  args.c
  core/          cpu.c  bus.c  machine.c  loader.c  elf.c
  isa/           isa.h                  IsaOps 接口
  isa/riscv64/   isa_riscv64.c  decode.c  execute.c  csr.c  mmu.c
  machine/       machine.h  spike_min.c  virt.c
  device/        device.h  ram.c  htif.c  uart16550.c  clint.c  plic.c  sifive_test.c
  host/          host.h  console_win.c  display_win.c  file_win.c  time_win.c
  util/          log.c  type.h
```

## 四、ISA 接口

```c
typedef struct IsaOps {
  const char *name;
  void (*Init)(CpuState *cpu);
  void (*Step)(CpuState *cpu);                        // 取指译码执行一条
  void (*RaiseIrq)(CpuState *cpu, int line, int level);
  void (*DumpRegs)(const CpuState *cpu);
} IsaOps;

extern const IsaOps kIsaRiscv64;   // 将来 kIsaX86、kIsaArm、kIsaMips
```

运行时选 ISA：ELF 读 e_machine 自动分派，.bin 由命令行指定。一个 exe 内所有 ISA 同时在场。x86 接入日即通用性审计日，core 里残留的 riscv 假设会让编译失败。

## 五、两台机器

机器只是一张设备表加复位状态，两台机器共用同一 riscv64 实现，证明机器与 ISA 正交。

| 机器 | 设备 | 用途 |
|---|---|---|
| spike_min | RAM 0x80000000 + HTIF | 阶段 1 跑 riscv-tests。tohost 地址从 ELF 符号表读，spike 同款约定 |
| virt | DRAM 0x80000000、CLINT 0x02000000、PLIC 0x0c000000、UART16550 0x10000000、test 0x100000 | 阶段 2 起 OpenSBI。权威来源 QEMU 源码 hw/riscv/virt.c |
| x86_min | 1MB RAM、UART16550 0x3F8、debug-exit 端口 | 阶段 1.5 专用，见第六节。证明同一套 core/设备层能装第三种 ISA |

对照真机：`qemu-system-riscv64 -M spike -bios none -kernel rv64ui-p-add.elf`，同一条测试两边跑。

## 六、阶段计划

### 阶段 0：工具链与素材
- 已有：mingw64 gcc 8.1 + mingw32-make + objcopy/objdump，编 cemu 本体够用；riscv-tests、OpenSBI 源码。
- 待办 1：下载 `xpack-riscv-none-elf-gcc-15.2.0-1-win32-x64.zip`，解压到固定路径，加入 PATH。darwin-x64 是 macOS 构建，Windows 不可用。
- 待办 2：riscv-tests 的 env/ 目录为空（crt.S、entry.S 丢失），构建前需重新拉取或补齐。
- LLVM：本阶段不需要。guest riscv 由 xpack gcc 覆盖，x86/arm/mips 阶段再引 LLVM 做多目标交叉。
- riscv-tests 构建：configure 后 mingw32-make isa；不成就手工脚本逐条编，每条测试只有几个源文件。
- OpenSBI 构建：mingw32-make PLATFORM=generic；不成则从 QEMU 安装包提取现成固件。

### 阶段 1：ISA 一致性
- 交付：util、host 控制台、core 的 bus/elf/loader/machine、isa/riscv64、device 的 ram/htif。
- ISA 范围：RV64IMAFDC + Zicsr + Zifencei + M 态 CSR，以 isa 套件覆盖为准。Zb* 等扩展后补。
- 语义来源：xiangshanNEMU 的 def_EHelper 逐条翻译，分歧查 RISC-V 手册裁决。
- 验收：rv64ui、rv64um、rv64ua、rv64uc、rv64uf、rv64ud、rv64mi 全绿，结果经 HTIF 上报。
- 已知坑：F 寄存器 NaN boxing、非对齐访存语义、mcause 与 mtval 的精确值、tohost 地址发现。

### 阶段 1.5：x86 最小证明（抽检抽象层，2026-08-30 决定）

目的：证明 IsaOps 抽象层真的能装下第二种 ISA。只做最简 386 实模式，不求全集，测试从简。

**步骤（顺序即验收阶梯）**
1. 把 spike 机器从 core/machine.c 拆到 machine/spike_min.c（计划内目录本来就有；kSpikeDramBase 等常量随迁）。core/machine.c 只剩 Machine 结构与运行循环。
2. 新建 src/isa/x86/，实模式子集 + kIsaX86（elf_machine=EM_386=3）进 kIsaTable。**验收标准：core/、device/、host/ 一个字不改编译通过 = 结构证明。**
3. 行为冒烟：bin 加载在 0x7C00，COM1 输出 + debug-exit 退出，同一镜像在 QEMU 双跑对拍输出与退出码。
4. 验收：kvm-unit-tests 的 386 级子集（实模式 + 基本指令）全绿。手写 asm 只能当冒烟探针（同 test/probe/ 性质），不算验收件。

**x86_min 机器契约（全部真实协议，无自造）**
- 内存：1MB 实模式 RAM；镜像 .bin 加载 0x7C00，复位态 CS:IP=0000:7C00、DL=0x80（BIOS boot sector 交接契约，QEMU seabios 引导到达同一时刻；对拍时注意 DL 等寄存器差异）。
- 输出：COM1 = UART16550 @ 0x3F8。uart16550 设备层本来 ISA 无关，先在这里落地一次，阶段 2 的 virt 机器在 0x10000000 复用同一设备——这就是设备解耦的行为证明。
- 退出：QEMU isa-debug-exit 端口写一字节退出。默认端口 0x501、退出码=写入值+1，**实现时用 `qemu-system-i386 -device isa-debug-exit,help` 实测校准，不凭记忆写死**。
- 中断控制器不做（386 最小集无 PIC 也能跑裸机）；异常走 IVT（非法指令 → int 6 自然产生）。

**范围**：只做实模式（段=(seg<<4)+off），不做保护模式/分页/FPU（387 本是外置）。难点全部在译码：modrm/SIB/前缀（66/67/段前缀/rep/lock）、同一指令 16/32 位双形态。估计 1500-2500 行（tiny386 全集 5250 行）。ELF 路线需要 ELF32（D9），证明阶段走 bin 路线绕开。

**参考实现**
- `tiny386/i386.c`（5250 行）+ `i386ins.def`：主语义 oracle，纯 C 386 解释器，正是这个范围。
- `nemu/src/isa/x86`：第二参照（real mode + 保护模式子集）。
- `v86/tests/kvm-unit-tests`（in-tree）：x86 世界的 riscv-tests，KVM 项目官方裸机套件，自报 pass/fail，验收就用它的 386 级子集；`v86/tests/nasm` 与 `v86/tests/qemu`（同镜像与 QEMU 对比）是方法论参照。
- **`dearchap-tinyemu/x86_cpu.c` 只剩 96 行残桩，不是参照。**

**工具链待办**：16/32 位 x86 测试镜像的构建手段。候选：nasm -f bin（实模式引导扇区最直接，需下载 nasm）；mingw gcc -m32 + objcopy -O binary（32 位平镜像）；若要走 ELF32 则先销 D9。kvm-unit-tests 的构建脚本假定 Linux gcc -m32，Windows 上大概率要手工编单文件，届时逐个看。

### 阶段 2：真实固件
- 交付：machine/virt.c、uart16550、clint、plic、sifive_test、M 态完整化（misa、medeleg、mideleg、PMP）。
- 验收：OpenSBI 起动，banner 从 UART 出来，进入 S 态等待下一级。UART 即控制台。
- 之后引 Linux：`-bios fw_jump -kernel vmlinux console=ttyS0`，帧缓冲和键盘可再后推。

### 阶段 3：cesdk
- 交付：crun 运行时（_start、putch→UART、halt→sifive_test）、klib、链接脚本、构建前端（先批处理形态）。
- 验收：hello 的 .bin 与 .elf 在 cemu 与真 QEMU 上行为一致。
- 蓝本：AM 源码逐文件对照移植。

### 阶段 4：x86（全集）
- 阶段 1.5 已落地实模式子集。此处补：32 位保护模式（GDT/描述符/特权级/门）、386 分页、PC 设备模型全集。
- bin 路线：multiboot 等价的入口契约，xv6 去掉 bootasm.S 与 bootmain.c 编为 .bin，cemu 负责入口状态与 MP 表。
- SeaBIOS 路线：官方 bios.bin 映射到内存顶端，复位 F000:FFF0。
- 两路线共享 PC 设备模型：CGA、PS/2、PIC、PIT、UART（复用 1.5 的 uart16550）、IDE、LAPIC、IOAPIC。
- 验收阶梯：xv6-x86 → Linux（参照 v86/tests/full 的"真实 OS 引导"清单）。

### 阶段 5：arm 与 mips
- 先引 LLVM 做交叉。本地无解释型参考，语义来源为官方手册。

## 七、复用映射

| 来源 | 拿什么 | 备注 |
|---|---|---|
| nemu | cpu-exec 循环骨架、IOMap 回调思想 | 框架可抄，AM 的设备地址表丢弃 |
| nemu/src/isa/x86 | x86 实模式语义第二参照 | riscv64 是空壳符号链接，实文件在 riscv32 目录 |
| xiangshanNEMU | riscv 语义 oracle（priv.c/timer.c/trigger.c/mmu.c） | 逐条翻译 |
| tiny386/i386.c + i386ins.def | x86 386 主语义 oracle | 纯 C，5250 行，正好是 1.5 的范围 |
| v86/tests/kvm-unit-tests | x86 验收套件 | in-tree，386 级子集 |
| dearchap-tinyemu | 映射树结构、iomem is_ram、uart16550/clint/plic 的 C 实现、htif 应答语义 | MIT 许可；**x86_cpu.c 是 96 行残桩，勿用** |
| abstract-machine | trm 模式、linker.ld、klib | 阶段 3 cesdk 的蓝本 |

## 八、测试阶梯

0. 冒烟：三条命令编五条指令的 .bin，HTIF 退出。
1. riscv-tests isa 套件全绿。
1.5. x86_min：0x7C00 bin 双跑对拍（cemu vs qemu-system-i386）→ kvm-unit-tests 386 级子集全绿。
2. OpenSBI 起动出 banner。
3. Linux 引导、cesdk hello。

每一级都是真实软件，无 cemu 定制成分。

## 九、当前状态与下一步（2026-08-31 第二轮更新，供后续会话接续）

- **阶段 1 完成 + 准则审查第一轮完毕**：同前。riscv 侧 127 项全绿，构建零告警。计数器语义裁决、抽象层现状见 准则审查.md。
- **阶段 1.5（x86 最小证明）进度**：
  1. ✅ spike 机器拆到 machine/spike_min.c（MachineCreate(name,opts) 分发、loader 收窄 Bus、IsaOps.bin_uses_htif、CpuState.image_base）。core/ 零改动编译通过 = 结构证明。
  2. ✅ src/isa/x86/ 实模式解释器 + kIsaX86（EM_386=3）进 kIsaTable。含 PM 平段入口（multiboot 契约：EAX=0x2BADB002、内置平 GDT、CR0.PE）、段描述符缓存（**翻译恒用缓存 base——PE=0 后缓存存续即 big real mode，这是关键机制**）、open-bus 内存语义（未解码地址读全 1 写丢弃）、串操作 + REP、IVT 异常分发、GRP8/setcc/BT 族。CpuState.io = 独立 I/O 总线（x86_min 未认领端口读全 1）。
  3. ✅ 冒烟双跑：test/smoke/x86_smoke.asm（nasm -f bin），cemu 与 qemu-system-i386 串口逐字节一致、退出码同（实测校准 isa-debug-exit：**status=(v<<1)|1 不是 v+1**）。设备：uart16550（dearchap SerialState 语义，TX-only）、debug_exit。
  4. ✅ **realmode（kvm-unit-tests 官方实模式套件）双跑对拍通过**。test/realmode/build.sh 把它编成 ELF32（mingw 适配：.pushsection 平展、-fno-leading-underscore、自备 lds、e_entry 补丁）。本轮定位并修掉三个真实解释器 bug：
     - **SIB 解码丢 index×scale**：`DecodeModrm` 在 `base==5 && mod==0` 分支只取 disp32，把 `index×scale` 项整个丢了，导致 `lea 0(,%eax,4)` 恒为 0。这让 regs_equal 里 `p1[i]` 的地址全算成 p1[0]，于是“掩码跳过 EAX 但测试改了 EAX”的用例（push/pop、mov、sub、pio、jcc、call）全在比 EAX 而挂——正好覆盖上一轮全部 23 个 FAIL。修后 index×scale 无条件累加，仅基址寄存器缺席。
     - **单字节 0x9A（CALL ptr16:16 立即远调用）未实现**：落到 illegal → #UD → 走 IVT[6]（suite 设 idt base=0，表项是 multiboot 头字节）乱跳进零区空转，表现为 call far 2 处假死。补 0x9A（照 FF /3 间接远调用同款：压 CS/返回 IP、载 CS、跳偏移）；顺带 0x9B FWAIT 在无 x87 模型下无可见效果当 nop。
     - **DoInt 把软件中断当 fault 压栈**：`DoInt` 一律压 `s->eip`（当前指令地址）。软件中断（INT n/INT3/INTO）属 trap 应压返回地址（`f.ip`），异常（#DE/#UD）属 fault 才压故障指令。给 DoInt 加 `ret_eip` 参数区分调用点后，test_int 的 `int 0x11 → IDT[0x11]=0x1000 处 0xCF(IRET) → 返回 int 之后` 才走得通（之前 IRET 忠实弹回被压错的 int 自身地址 → 无限重执）。
     - 另：RF（EFLAGS bit16）按瞬态建模——iret/popf 载入时屏蔽 RF|VM，与真机经 pushfl 不可观测为 1 一致（修掉 rflags.rf 用例）。
  - **对拍结果**：`diff cemu_out qemu_out` → `69a70,110`，即 cemu 的 69 行与 QEMU 前 69 行**逐字节一致**（无任何改动行）。QEMU 110 PASS/0 FAIL；cemu 跑到第 70 项 test_sti_inhibit 的 `hlt` 处停（exit=0，非超时非挂死）。**该 hlt 需要周期性定时器中断唤醒**（QEMU 默认带 PIT+PIC 发 IRQ0 唤醒 hlt），属阶段 2 设备模型（8253 PIT + 8259 PIC + 中断唤醒），非解释器 bug。可达子集 69 PASS/0 FAIL、双跑逐行一致即本轮验收。
- 工具链：nasm 2.16.03 在 D:/SSDOWN/tools/nasm-2.16.03/；qemu-system-i386.exe 在 D:/qemu（-device isa-debug-exit,iobase=0xf4,iosize=0x4 对拍用）；mingw gcc -m32 可出 pe-i386 目标（无 32 位链接库，平二进制走 objcopy）。
- 回归：`bash test/run.sh`（127 riscv）；冒烟 `build/cemu.exe --machine x86 --isa x86 test/smoke/x86_smoke.bin`；realmode `build/cemu.exe --machine x86 --isa x86 test/realmode/realmode.elf`（trace：`CEMU_TRACE=1`，现打印全 8 GPR）。
- git：main 分支（本轮新检查点见 git log）。test/realmode/ 下的 realmode.s/realmode.elf/probe_harness.c 为构建/诊断产物（已入库）；rc.*/probe.*/realmode_cut.c/*.txt 等本地诊断由 .gitignore 挡住。
- **下一步**：阶段 1.5 实模式解释器已双跑验收。剩余 41 项 realmode 用例被 hlt-wake 设备缺口挡住，自然并入阶段 2 PC 平台（PIT+PIC）时一并打通；或直接按原计划进 阶段 2（virt/OpenSBI）。
